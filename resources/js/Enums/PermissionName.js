export default Object.freeze({

    /*
     * When this enum is modified, the enum "app/Enums/PermissionName.php"
     * have to be changed too for consistency with Laravel.
    */

    GET: 'get',
    SET: 'set'
});
